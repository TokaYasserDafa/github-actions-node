on:
  workflow_dispatch:

jobs:
  example:
    runs-on: ubuntu-latest

    steps:
      - name: Set API Key
        run: echo "API_KEY=12345Y" >> $GITHUB_ENV

      - name: Use API Key
        run: echo "Using API_KEY: ${{ env.API_KEY }}"

