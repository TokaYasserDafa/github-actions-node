on:  
  workflow_dispatch:

jobs:
  dockerized-job:
    runs-on: ubuntu-latest
    container:
      image:ghcr.io/my-org/my-private-image:latest
      credentials:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    steps:
      - run: node --version
      - run: echo "this whole job runs in the nodejs container"