on:
  workflow_dispatch:
jobs:
  job_1:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: |
          npm install
          npm run build
        working-directory: ./src
        shell: bash
        #pwsh , cmd , powershell , bash , sh , python , ruby , php , perl , node , Rscript , groovy , java , kotlin , scala , haxe , dart

      - run: print %ENV
        shell: perl {0}
