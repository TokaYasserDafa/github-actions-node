name: Incident Report

on:
  repository_dispatch:
    types:
      - incident_report

jobs:
  handle-incident:
    runs-on: ubuntu-latest

    steps:
      - name: Print event
        run: echo "Incident report received!"